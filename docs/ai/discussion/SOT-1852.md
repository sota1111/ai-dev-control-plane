# Discussion Thread: SOT-1852

- Created: 2026-07-22T14:45:45Z
- Participants: codex:sol, claude:fable
- Max rounds: 2

## Topic

# 議題: 松(matsu)・竹(take)・梅(ume)・zero のデッキ候補プール再編（削減+追加）

最強デッキ探索のため、4リポジトリのデッキ候補プールを再編する。**各リポジトリについて「削減するデッキ」「追加するデッキ」「その数」「理由（多様性・今後の戦略）」を具体的に合意せよ。**

## インプット1: Kaggle 収束スコア (2026-07-22 時点, pokemon-tcg-ai-battle)

| agent | score | 備考 |
|---|---|---|
| matsu | 557.2 | MCTS (決定化, ref 54811671, 07-18) — 首位 |
| fable | 550.7 | 決定化MCTS+竹式ルール (ref 54883092) |
| sol | 536.8 | semantic MCTS (ref 54894166) |
| debate | 523.6 | fable champion 移植 (ref 54900084) |
| zero | 507.4 | AlphaZero系・model未同梱 minimal loop (ref 54904768) |
| take | 482.5 | ルールベース (ref 54904755) — 完走組で最下位 |
| ume | ERROR×2 | PPO+MCTS (ref 54904762/54904924)。post-fix ビルドでもERROR、ローカル再現不能。深掘り修正は SOT-1855 の担当 |

## インプット2: 各リポジトリの候補プール現状

**共通:** matsu/take/ume は同一の「大会実績25デッキ」プール(`decks/initial/`, SOT-1684配布)を持つ。内訳: Dragapult系5種(01-05,22), Hydrapple(06), N's Zoroark系(07,24), Ogerpon Box(08), Slowking系(09,23), Hop's Trevenant(10), Lillie's Clefairy系(11,21), Alakazam(12), Festival Lead(13), Mega Lucario ex(14), Marnie's Grimmsnarl ex(15), Crustle(16), Rocket's Mewtwo ex(17), Rocket's Honchkrow(18), Ethan's Typhlosion(19), Cynthia's Garchomp ex(20), Mega Lopunny ex(25)。エンジンにCRI拡張が無く 15/25 デッキに計20枚の代替カード置換あり(manifest.json 記録)。

- **matsu**: champion=deck.csv(SOT-1721 提出時 hash 42068a180390)。デッキ間の対戦成績表は無し(bench_configs はエージェント設定のA/B用)。25デッキはmirror環境として使用。
- **take**: 独自候補4種 `decks/`(aggro/balanced/lowenergy/tempo_metal) は SOT-1662 で全て champion に有意負け(勝率 0.260/0.325/0.175/測定済, N=200, CI上限<0.5)。champion=35エネルギー高積載デッキ。敗因分析(SOT-1694): 負けの71%がサイド取り切られ(prize-out)。`eval/deck_validator.py` と `eval/compare_decks.py`(N=200 が数分) あり。
- **ume**: champion_v1=水6たね+水エネ35(registry.json v1, SOT-1651)。vs Rule 勝率 0.56 CI[0.49,0.63] と辛勝レベル。`eval/deck_eval.py`(ペアA/B, N=200 約7秒と高速) と 25デッキ rotation gate あり。
- **zero**: **候補プールが存在しない**。deck.csv 1枚のみ(take/ume champion と同系の 721/722/723 系+エネ35)。デッキ比較ハーネスも無し(arena はモデル対モデル)。

## インプット3: 制約・今後の文脈

- 本Issue(SOT-1852)で実装可能な範囲で合意すること: 候補プール(`decks/`配下)の削減・追加、必要なら軽量な検証実行。**champion(deck.csv) の変更は screen→confirm で incumbent に勝った場合のみ**(集約 Wilson CI 下限>0.5, fault 0 必須)。
- アルゴリズム改善は兄弟Issue(SOT-1853 matsu敗因/SOT-1854 take trace/SOT-1855 ume蒸留+ERROR/SOT-1856 zero学習スケール)の担当。本Issueはデッキプールのみ。
- 追加デッキの入手源の選択肢: (a) 既存25デッキプールの variant(エネ比・テック差替), (b) 他repoのchampion/強デッキの移植, (c) control-plane の合法デッキ生成器(`src/lib/ptcgDeck.ts`, KAGGLE_DECK_RULES: 60枚/同名≤4/ACE SPEC≤1/たね≥1)による新規生成。追加デッキは各repoのvalidatorでルール適合を確認する。
- 計測予算: 大規模 confirm は限られる。screen(small-N)優先。ume の deck_eval は高速(N=200≈7秒)なので ume が最も実験回転が速い。
- 参考(過去の計測): fable の 26候補 screen→confirm では 松竹梅系 champion(26_stw)が 0.8165 で追認(SOT-1794)。matsu と fable の deck.csv は同一リスト。

## 合意すべき出力(## Conclusion に必ず含めること)

各リポジトリごとに:
1. **削減**: 対象デッキ(ファイル名で列挙)と本数
2. **追加**: 追加デッキ(アーキタイプ・入手源(a)/(b)/(c)を明記)と本数
3. **理由**: 多様性・今後の戦略の観点から1-2行
加えて **全体戦略**(4repoのプールをどう差別化し、次の screen→confirm/提出につなげるか)を3-5行。

## Round 1 — codex:sol (2026-07-22T14:46:41Z)

## Position
初期25本を一律に残すより、重複アーキタイプを整理し、各repoの役割を分けるべきである。matsuは広い対戦相手集合、takeはprize-out改善、umeは高速な比率探索、zeroは最小限の学習用プールに特化させる。なお `deck.csv` は候補プール外のincumbentとして削減対象に含めず、昇格条件も変更しない。

## Rebuttal
none

## Stance: DISAGREE

## Conclusion
- **matsu**
  - **削減（6本）:** `decks/initial/03.csv`, `04.csv`, `05.csv`（Dragapult重複）、`21.csv`（Clefairy重複）、`23.csv`（Slowking重複）、`24.csv`（N's Zoroark重複）。
  - **追加（4本）:** take/ume型35エネルギー高積載デッキ1本（b）、`01` Dragapultの低エネ型・中エネ型2本（a）、`17` Rocket's Mewtwoのテック差替型1本（a）。
  - **理由:** 首位MCTSの汎化確認用としてアーキタイプ幅は維持しつつ、同系重複をエネルギー曲線と別repo championとの比較へ置き換える。

- **take**
  - **削減（12本）:** `decks/aggro.csv`, `balanced.csv`, `lowenergy.csv`, `tempo_metal.csv`（有意敗北済み4本）、および `decks/initial/02.csv`, `03.csv`, `04.csv`, `05.csv`, `21.csv`, `23.csv`, `24.csv`, `22.csv`（重複系8本）。
  - **追加（6本）:** matsu champion移植1本（b）、matsu championのエネルギー増減2本（a）、高打点たね中心のprize-race型2本（c）、非ex中心のsingle-prize型1本（c）。
  - **理由:** 敗北済み独自候補を温存せず、71%を占めるprize-outに直接効く速度・打点・サイド交換効率へ探索を集中する。

- **ume**
  - **削減（8本）:** `decks/initial/02.csv`, `03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`（同一アーキタイプ重複）。
  - **追加（8本）:** matsu champion移植1本（b）、そのエネルギー比3段階3本（a）、Dragapult低エネ型1本（a）、Rocket's Mewtwoテック型1本（a）、高速たねaggro型1本（c）、single-prize control型1本（c）。
  - **理由:** N=200約7秒の評価速度を活かし、他repoより広い比率・テック探索を担当する。ERROR修正やアルゴリズム変更はSOT-1855へ分離する。

- **zero**
  - **削減（0本）:** 候補プールが存在しないため無し。`deck.csv` はincumbentなので削除しない。
  - **追加（8本）:** matsu champion1本（b）、take/ume champion型1本（b）、Dragapult代表`01`・Hydrapple`06`・Ogerpon Box`08`・Rocket's Mewtwo`17`の4本（b）、matsu champion低エネ型1本（a）、single-prize型1本（c）。
  - **理由:** AlphaZero系学習で相手分布が単一化するのを避けつつ、比較ハーネス未整備でも扱える小さな代表集合に留める。

**全体戦略:** 追加全候補を各repoのvalidatorで検証し、60枚・同名4枚以下・ACE SPEC 1枚以下・たね1枚以上を満たさないものはscreen前に除外する。matsuは堅牢性、takeはprize-race、umeは高速探索、zeroは学習分布の最小多様化という役割に分ける。まずsmall-N screenを実施し、umeで有望な比率を素早く絞ってmatsu/takeへ横展開する。`deck.csv` の更新は集約Wilson CI下限>0.5かつfault 0のconfirm通過時だけ行い、それ以外は候補追加に留める。

## Round 1 — claude:fable (2026-07-22T14:48:56Z)

## Position
- sol の役割分担(matsu=堅牢性/take=prize-race/ume=高速比率探索/zero=学習分布の最小多様化)と削減・追加の規模感は支持する。ただし repo 実測で3つの事実誤認を確認したので修正が必要。
- **実測①: matsu/take/ume の deck.csv は byte-identical**(diff で確認)。よって sol 案の「matsu champion 移植を take/ume へ(b)」「take/ume 型35エネ高積載を matsu へ(b)」は既存 incumbent と同一デッキを追加する死にスロットである。zero の deck.csv だけは同系だが別リスト(60枚中構成差あり)なので、zero への matsu champion 移植のみ意味があるが、sol 案の zero への「matsu champion」+「take/ume champion 型」の2本は同一デッキの二重追加になる。
- **実測②: `02_raging_bolt_ogerpon.csv` は Dragapult 重複ではなく独立アーキタイプ**(Raging Bolt/Ogerpon)。take/ume の削減リストから 02 を外すべき。
- **実測③: ume の `eval/bench_deck_rotation.py`(fault 安全ゲート)と `eval/selfplay.py` の rotation pool は `decks/initial/` を直接参照**。物理削除は fault ゲートのカバレッジと self-play 分布(SOT-1855/1856 の担当領域)を暗黙に変える。削減は削除ではなく `decks/archive/` への移動とし、安全ゲートは全25枚で回せる状態を残し、候補 screen だけ整理後プールを使う。

## Rebuttal
sol の結論への具体的応答: (1) champion 移植スロット3本(matsu への35エネ型、take/ume への matsu champion)は同一ファイルの再追加で計測予算の無駄 — 実在の差分がある zero への移植だけ残し、他は生成器(c)または zero champion 移植(b, 実際に別リスト)へ差し替えるべき。(2) 02 を「重複系」として削るのはアーキタイプ多様性を自ら削る誤りで、削減対象は Dragapult 系(03/04/05,22)・Clefairy(21)・Slowking(23)・Zoroark(24) の真の重複に限定すべき。(3) 削除でなく archive 移動なら、ume の rotation fault ゲート・self-play 分布・manifest.json の履歴を壊さずに候補プールを絞れる。この3修正を統合した下記 Conclusion を提案する — sol が次ラウンドで採用すれば AGREE する。

## Stance: DISAGREE

## Conclusion
**共通方針:** 「削減」は物理削除ではなく各 repo の `decks/archive/` への移動とする(fault 安全ゲート・manifest 履歴・self-play 分布を保全し、候補 screen は整理後の `decks/initial/`+新候補のみを対象にする)。追加デッキは全て各 repo の validator(無い repo は control-plane `src/lib/ptcgDeck.ts` の KAGGLE_DECK_RULES: 60枚/同名≤4/ACE SPEC≤1/たね≥1)で適合確認してから登録する。`deck.csv`(incumbent)は削減対象外で、変更は screen→confirm で集約 Wilson CI 下限>0.5 かつ fault 0 の場合のみ。

- **matsu**
  1. **削減(6本):** `decks/initial/03_dragapult_blaziken.csv`, `04_dragapult_dusknoir.csv`, `05_dragapult_dudunsparce.csv`(Dragapult重複), `21_lillie_s_clefairy_ex_naic_champion.csv`, `23_slowking_naic_4th.csv`, `24_n_s_zoroark_ex_naic_10th.csv`(NAIC重複) → `decks/archive/` へ。
  2. **追加(4本):** `01` Dragapult の低エネ型・中エネ型2本(a)、`17` Rocket's Mewtwo のテック差替型1本(a)、生成器による single-prize 型1本(c)。※「take/ume 型35エネ高積載」は matsu 自身の champion と同一のため追加しない。
  3. **理由:** 首位 MCTS の汎化確認用にアーキタイプ幅を維持しつつ、同系重複をエネルギー曲線と未カバーの single-prize 軸に置き換える。

- **take**
  1. **削減(11本):** `decks/deck_aggro.csv`, `deck_balanced.csv`, `deck_lowenergy.csv`, `deck_tempo_metal.csv`(SOT-1662 で有意敗北済み4本)+ `decks/initial/03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`(重複7本。02 Raging Bolt Ogerpon は独立アーキタイプのため残す) → `decks/archive/` へ。
  2. **追加(6本):** zero champion の移植1本(b, matsu/take/ume champion と別リストであることを diff で確認済)、champion のエネルギー増減2本(a)、高打点たね中心の prize-race 型2本(c)、非 ex 中心の single-prize 型1本(c)。
  3. **理由:** 敗北済み候補を温存せず、負けの71%を占める prize-out に直接効く速度・打点・サイド交換効率へ探索を集中する。

- **ume**
  1. **削減(7本):** `decks/initial/03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`(重複7本。02 は残す) → `decks/archive/` へ。rotation fault ゲート(`eval/bench_deck_rotation.py`)は `--deck-dir` で archive 込み全25枚を指定して従来通り実行可能とする。
  2. **追加(8本):** zero champion の移植1本(b)、champion エネルギー比3段階3本(a)、Dragapult 低エネ型1本(a)、Rocket's Mewtwo テック型1本(a)、高速たね aggro 型1本(c)、single-prize control 型1本(c)。※matsu champion は ume 自身の champion と同一のため移植しない。
  3. **理由:** `deck_eval.py`(N=200≈7秒)の回転速度を活かし、4repo 中最も広い比率・テック探索を担当する。ERROR 修正・アルゴリズムは SOT-1855 へ分離。

- **zero**
  1. **削減(0本):** 候補プール不在のため無し。`deck.csv`(incumbent, matsu と別リスト)は据え置く。
  2. **追加(8本, `decks/candidates/` を新設):** matsu champion 1本(b, zero の現デッキと実差分あり)、Dragapult 代表 `01`・Hydrapple `06`・Ogerpon Box `08`・Rocket's Mewtwo `17` の4本(b)、matsu champion 低エネ型1本(a)、single-prize 型1本(c)、prize-race 型1本(c)。※「take/ume champion 型」は matsu champion と同一のため二重追加しない。
  3. **理由:** AlphaZero 系学習(SOT-1856)の相手/自己対戦分布が単一デッキに過適合するのを避けつつ、比較ハーネス未整備でも扱える小さな代表集合に留める。

**全体戦略:** 4repo の役割を matsu=堅牢性(広いアーキタイプ集合での汎化確認)、take=prize-race 特化、ume=高速な比率・テック探索、zero=学習分布の最小多様化に分ける。まず ume の高速 deck_eval で比率系候補を small-N screen して有望域を絞り、それを matsu/take の候補へ横展開して screen→上位のみ large-N confirm にかける。champion(deck.csv) の更新は集約 Wilson CI 下限>0.5 かつ fault 0 の confirm 通過時のみ行い、通過しない候補はプールに残して次回 screen の比較対象とする。matsu/take/ume の champion が現在同一である事実を踏まえ、重複移植を排して実差分のある候補だけに計測予算を使う。

## Round 2 — codex:sol (2026-07-22T14:49:39Z)

## Position
役割分担、真の重複だけを削減する対象選定、同一 champion の重複移植を排除する修正には同意する。ただし `decks/initial/` を直接読む ume の self-play と rotation gate は、重複7本を `archive/` に移動するだけでは従来の25本分布を保全できない。移動前の全25本を `decks/rotation_baseline/` に複製して固定し、fault gate はそこを `--deck-dir` に指定する必要がある。self-play の参照先変更はアルゴリズム領域に波及するため、本Issueでは変更せず、整理後候補を明示リストでscreenする。

## Rebuttal
fable の実測①・②は採用する。matsu/take/ume の同一 champion を相互移植する案は撤回し、`02_raging_bolt_ogerpon.csv` は独立アーキタイプとして残す。一方、実測③から「archiveへの移動だけでself-play分布も保全できる」という結論は導けないため、全25本の固定baselineを別途残す修正が必要である。

## Stance: DISAGREE

## Conclusion
**共通方針:** 「削減」は active screen 対象から外して `decks/archive/` へ移動することを意味し、履歴は削除しない。ume は移動前の全25本を `decks/rotation_baseline/` に複製して固定し、rotation fault gate は同ディレクトリを `--deck-dir` に指定する。self-play の参照・分布変更は SOT-1855/1856 に委ね、本Issueのscreenは整理後候補を明示リストで指定する。全追加候補を各repoのvalidator、または control-plane の KAGGLE_DECK_RULESで検証する。`deck.csv` は削減対象外で、集約Wilson CI下限>0.5かつfault 0のconfirm通過時のみ更新する。

- **matsu**
  1. **削減（6本）:** `decks/initial/03_dragapult_blaziken.csv`, `04_dragapult_dusknoir.csv`, `05_dragapult_dudunsparce.csv`, `21_lillie_s_clefairy_ex_naic_champion.csv`, `23_slowking_naic_4th.csv`, `24_n_s_zoroark_ex_naic_10th.csv`をarchiveへ移動。
  2. **追加（4本）:** `01` Dragapult低エネ型・中エネ型2本(a)、`17` Rocket's Mewtwoテック差替型1本(a)、single-prize型1本(c)。
  3. **理由:** 広いアーキタイプ幅を保ち、重複をエネルギー曲線とsingle-prize軸へ置換して首位MCTSの汎化を確認する。

- **take**
  1. **削減（11本）:** `decks/deck_aggro.csv`, `deck_balanced.csv`, `deck_lowenergy.csv`, `deck_tempo_metal.csv`と、`decks/initial/03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`をarchiveへ移動。`02`は残す。
  2. **追加（6本）:** zero champion 1本(b)、incumbentのエネルギー増減2本(a)、高打点たね中心prize-race型2本(c)、非ex single-prize型1本(c)。
  3. **理由:** 有意敗北済み候補を外し、敗戦の71%を占めるprize-outに対して速度・打点・サイド交換効率を探索する。

- **ume**
  1. **削減（7本）:** `decks/initial/03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`をarchiveへ移動。`02`は残す。全25本の固定コピーは`decks/rotation_baseline/`に保持する。
  2. **追加（8本）:** zero champion 1本(b)、incumbentのエネルギー比3段階3本(a)、Dragapult低エネ型1本(a)、Rocket's Mewtwoテック型1本(a)、高速たねaggro型1本(c)、single-prize control型1本(c)。
  3. **理由:** 高速な`deck_eval.py`を活かして最大幅の比率・テック探索を担当し、ERROR・アルゴリズム修正はSOT-1855へ分離する。

- **zero**
  1. **削減（0本）:** 候補プールがないため無し。既存`deck.csv`は維持する。
  2. **追加（8本）:** `decks/candidates/`を新設し、matsu champion 1本(b)、Dragapult `01`・Hydrapple `06`・Ogerpon Box `08`・Rocket's Mewtwo `17`の4本(b)、matsu champion低エネ型1本(a)、single-prize型1本(c)、prize-race型1本(c)。
  3. **理由:** AlphaZero系の単一デッキ過適合を避けながら、比較ハーネス未整備でも管理可能な代表集合に留める。

**全体戦略:** matsuは広域な堅牢性、takeはprize-race、umeは高速な比率・テック探索、zeroは学習用の最小多様性を担当する。まずumeでsmall-N screenを行い、有望候補をmatsu/takeへ横展開する。各repoで上位候補だけをlarge-N confirmし、集約Wilson CI下限>0.5かつfault 0の場合のみchampionへ昇格させる。同一のmatsu/take/ume championは相互移植せず、実差分のあるzero championとvariantへ計測予算を使う。

## Round 2 — claude:fable (2026-07-22T14:50:11Z)

## Position
sol は私の実測①(matsu/take/ume の champion deck.csv が byte-identical であるため相互移植は死にスロット)と実測②(`02_raging_bolt_ogerpon.csv` は独立アーキタイプであり削減対象から除外)を全面採用し、削減リスト・追加リストを私の round 1 案と同一に修正した。実測③への sol の refinement — archive 移動だけでは `decks/initial/` を直接参照する ume の rotation gate/self-play の従来25本分布は保全されないので、移動前の全25本を `decks/rotation_baseline/` に複製固定して fault gate はそこを `--deck-dir` 指定し、self-play の参照変更は SOT-1855/1856 へ委ねる — は、私の「archive 込みで指定」案より明示的で再現性が高く、正しい指摘として受け入れる。これで両者の結論は実質一致した。

## Rebuttal
反論なし。唯一残っていた相違点(ume の25本分布の保全方法)は sol の `decks/rotation_baseline/` 固定複製案の方が優れており採用する。以下の Conclusion は sol の round 2 の結論をそのまま共同回答として受諾したものである。

## Stance: AGREE

## Conclusion
**共通方針:** 「削減」は active screen 対象から外して `decks/archive/` へ移動することを意味し、履歴は削除しない。ume は移動前の全25本を `decks/rotation_baseline/` に複製して固定し、rotation fault gate は同ディレクトリを `--deck-dir` に指定する。self-play の参照・分布変更は SOT-1855/1856 に委ね、本Issueのscreenは整理後候補を明示リストで指定する。全追加候補を各repoのvalidator、または control-plane の KAGGLE_DECK_RULESで検証する。`deck.csv` は削減対象外で、集約Wilson CI下限>0.5かつfault 0のconfirm通過時のみ更新する。

- **matsu**
  1. **削減（6本）:** `decks/initial/03_dragapult_blaziken.csv`, `04_dragapult_dusknoir.csv`, `05_dragapult_dudunsparce.csv`, `21_lillie_s_clefairy_ex_naic_champion.csv`, `23_slowking_naic_4th.csv`, `24_n_s_zoroark_ex_naic_10th.csv`をarchiveへ移動。
  2. **追加（4本）:** `01` Dragapult低エネ型・中エネ型2本(a)、`17` Rocket's Mewtwoテック差替型1本(a)、single-prize型1本(c)。
  3. **理由:** 広いアーキタイプ幅を保ち、重複をエネルギー曲線とsingle-prize軸へ置換して首位MCTSの汎化を確認する。

- **take**
  1. **削減（11本）:** `decks/deck_aggro.csv`, `deck_balanced.csv`, `deck_lowenergy.csv`, `deck_tempo_metal.csv`と、`decks/initial/03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`をarchiveへ移動。`02`は残す。
  2. **追加（6本）:** zero champion 1本(b)、incumbentのエネルギー増減2本(a)、高打点たね中心prize-race型2本(c)、非ex single-prize型1本(c)。
  3. **理由:** 有意敗北済み候補を外し、敗戦の71%を占めるprize-outに対して速度・打点・サイド交換効率を探索する。

- **ume**
  1. **削減（7本）:** `decks/initial/03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`をarchiveへ移動。`02`は残す。全25本の固定コピーは`decks/rotation_baseline/`に保持する。
  2. **追加（8本）:** zero champion 1本(b)、incumbentのエネルギー比3段階3本(a)、Dragapult低エネ型1本(a)、Rocket's Mewtwoテック型1本(a)、高速たねaggro型1本(c)、single-prize control型1本(c)。
  3. **理由:** 高速な`deck_eval.py`を活かして最大幅の比率・テック探索を担当し、ERROR・アルゴリズム修正はSOT-1855へ分離する。

- **zero**
  1. **削減（0本）:** 候補プールがないため無し。既存`deck.csv`は維持する。
  2. **追加（8本）:** `decks/candidates/`を新設し、matsu champion 1本(b)、Dragapult `01`・Hydrapple `06`・Ogerpon Box `08`・Rocket's Mewtwo `17`の4本(b)、matsu champion低エネ型1本(a)、single-prize型1本(c)、prize-race型1本(c)。
  3. **理由:** AlphaZero系の単一デッキ過適合を避けながら、比較ハーネス未整備でも管理可能な代表集合に留める。

**全体戦略:** matsuは広域な堅牢性、takeはprize-race、umeは高速な比率・テック探索、zeroは学習用の最小多様性を担当する。まずumeでsmall-N screenを行い、有望候補をmatsu/takeへ横展開する。各repoで上位候補だけをlarge-N confirmし、集約Wilson CI下限>0.5かつfault 0の場合のみchampionへ昇格させる。同一のmatsu/take/ume championは相互移植せず、実差分のあるzero championとvariantへ計測予算を使う。

## Moderator — claude:fable (2026-07-22T14:50:38Z)

## Verdict
両者の Round 2 の Conclusion は文面まで一致しており（sol の Round 2 修正案を fable が verbatim で受諾）、形式上 sol の DISAGREE が同一ラウンドに残ったため合意宣言に至らなかっただけである。よってその共通結論を拘束力ある裁定として採択する。実測に基づく修正（matsu/take/ume champion の byte-identical 性による重複移植排除、`02_raging_bolt_ogerpon.csv` の独立アーキタイプ保持、ume の `decks/rotation_baseline/` 固定複製）がすべて反映された唯一の完成案であり、対立点は残っていない。

**採択される結論:**

**共通方針:** 「削減」は active screen 対象から外して `decks/archive/` へ移動することを意味し、履歴は削除しない。ume は移動前の全25本を `decks/rotation_baseline/` に複製して固定し、rotation fault gate は同ディレクトリを `--deck-dir` に指定する。self-play の参照・分布変更は SOT-1855/1856 に委ね、本Issueのscreenは整理後候補を明示リストで指定する。全追加候補を各repoのvalidator、または control-plane の KAGGLE_DECK_RULESで検証する。`deck.csv` は削減対象外で、集約Wilson CI下限>0.5かつfault 0のconfirm通過時のみ更新する。

- **matsu**
  1. **削減（6本）:** `decks/initial/03_dragapult_blaziken.csv`, `04_dragapult_dusknoir.csv`, `05_dragapult_dudunsparce.csv`, `21_lillie_s_clefairy_ex_naic_champion.csv`, `23_slowking_naic_4th.csv`, `24_n_s_zoroark_ex_naic_10th.csv`をarchiveへ移動。
  2. **追加（4本）:** `01` Dragapult低エネ型・中エネ型2本(a)、`17` Rocket's Mewtwoテック差替型1本(a)、single-prize型1本(c)。
  3. **理由:** 広いアーキタイプ幅を保ち、重複をエネルギー曲線とsingle-prize軸へ置換して首位MCTSの汎化を確認する。

- **take**
  1. **削減（11本）:** `decks/deck_aggro.csv`, `deck_balanced.csv`, `deck_lowenergy.csv`, `deck_tempo_metal.csv`と、`decks/initial/03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`をarchiveへ移動。`02`は残す。
  2. **追加（6本）:** zero champion 1本(b)、incumbentのエネルギー増減2本(a)、高打点たね中心prize-race型2本(c)、非ex single-prize型1本(c)。
  3. **理由:** 有意敗北済み候補を外し、敗戦の71%を占めるprize-outに対して速度・打点・サイド交換効率を探索する。

- **ume**
  1. **削減（7本）:** `decks/initial/03.csv`, `04.csv`, `05.csv`, `21.csv`, `22.csv`, `23.csv`, `24.csv`をarchiveへ移動。`02`は残す。全25本の固定コピーは`decks/rotation_baseline/`に保持する。
  2. **追加（8本）:** zero champion 1本(b)、incumbentのエネルギー比3段階3本(a)、Dragapult低エネ型1本(a)、Rocket's Mewtwoテック型1本(a)、高速たねaggro型1本(c)、single-prize control型1本(c)。
  3. **理由:** 高速な`deck_eval.py`を活かして最大幅の比率・テック探索を担当し、ERROR・アルゴリズム修正はSOT-1855へ分離する。

- **zero**
  1. **削減（0本）:** 候補プールがないため無し。既存`deck.csv`は維持する。
  2. **追加（8本）:** `decks/candidates/`を新設し、matsu champion 1本(b)、Dragapult `01`・Hydrapple `06`・Ogerpon Box `08`・Rocket's Mewtwo `17`の4本(b)、matsu champion低エネ型1本(a)、single-prize型1本(c)、prize-race型1本(c)。
  3. **理由:** AlphaZero系の単一デッキ過適合を避けながら、比較ハーネス未整備でも管理可能な代表集合に留める。

**全体戦略:** matsuは広域な堅牢性、takeはprize-race、umeは高速な比率・テック探索、zeroは学習用の最小多様性を担当する。まずumeでsmall-N screenを行い、有望候補をmatsu/takeへ横展開する。各repoで上位候補だけをlarge-N confirmし、集約Wilson CI下限>0.5かつfault 0の場合のみchampionへ昇格させる。同一のmatsu/take/ume championは相互移植せず、実差分のあるzero championとvariantへ計測予算を使う。
