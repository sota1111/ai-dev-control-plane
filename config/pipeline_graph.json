{
  "version": 1,
  "steps": [
    "task-check",
    "implementation",
    "verification",
    "acceptance",
    "github",
    "linear-report"
  ],
  "retry": {
    "max": { "env": "PIPELINE_MAX_DEBUG_CYCLES", "default": 2 }
  }
}
